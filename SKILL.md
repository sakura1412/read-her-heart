---
name: read-her-heart
description: Analyze whether a proposed product, business, or internal requirement is necessary, valuable, feasible, and appropriately prioritized, optionally by inspecting an authorized live system. Use when users ask whether a requirement should be built, what issues it has, or how to assess it; do not use for implementation planning alone.
---

# Read Her Heart

评估的目标不是替用户把想法合理化，而是给出可追溯的“做 / 不做 / 先验证再决定”建议。以用户给出的业务背景和经授权查看的系统事实为准；不自行编造市场数据、用户规模、技术能力或成本。

## 系统实查模式

当用户提供系统地址，并明确授权使用其提供的账号访问时，先通过浏览器登录系统，以实际页面、权限和流程补全需求上下文。仅执行为理解系统所必需的只读浏览；不得创建、编辑、删除、提交、发布、发送消息、导出数据，或改变任何系统设置。需要写入、下载或触发外部动作时，先取得该次操作的明确确认。

- 只接受用户明确提供的系统地址和登录信息；不要求用户把密码写入需求文档，也绝不将密码、令牌、验证码、Cookie 或个人/业务敏感数据写入 Skill、文件、截图、命令、报告或聊天复述中。
- 建议使用权限最小、仅用于演示或测试的账号。若账号权限超出评估所需范围，提醒用户换用只读或受限账号。
- 遇到 MFA、验证码、SSO 审批、访问受限或需要绕过安全机制时，停在该页面，请用户自行完成验证；不得尝试绕过。
- 只记录与评估直接相关的脱敏观察，例如页面名称、角色可见性、操作步骤、状态流转、字段类型、校验规则和现有替代路径。不要摘录真实客户信息、订单、身份证明、联系方式或商业机密。
- 系统页面和数据是当前环境的证据，不自动代表所有用户或生产规则；应标明所检查的账号角色、环境（生产/测试）和覆盖范围。

完成实查后，使用 [系统实查指南](references/system-inspection.md) 整理“当前系统事实”，再进入需求评估。若未提供系统访问条件，则按用户提供的资料做常规评估，并清晰说明系统层面的未知项。

## 工作方式

1. 先复述待评估需求、预期决策与已知背景。若启用系统实查模式，先梳理现有系统的角色、入口、关键任务流程、数据/状态、约束与相邻功能。将每一项判断标为“已知事实”“合理假设”或“待验证”。信息不足时，仍可基于条件给出初步结论，但必须指出改变结论的关键变量。
2. 从业务、用户、价值和可行性四个维度分析：
   - **业务（为什么做）**：与公司战略、产品定位、商业目标的关系；机会成本；不做的后果。
   - **用户（谁在用）**：目标用户画像、触发场景、现有任务链路、真实痛点与替代方案。区分用户提出的方案与其底层问题。
   - **价值（收益多大）**：对用户的时间、成功率、体验或风险的改善，以及对公司的拉新、留存、转化、收入或效率的影响。说明价值如何衡量。
   - **可行性（能否做）**：技术复杂度、依赖与数据条件、研发周期、运营/客服资源、隐私安全、法律合规、上线及长期维护风险。
3. 按问题性质选用方法，而非机械套用全部模型：
   - 用 **5W2H** 补齐需求边界、场景、方案、时间和成本收益。
   - 当需求像解决方案而非问题时，用 **5 Whys** 追溯根因，最多五层，结论应能被证据验证。
   - 当流程或范围不清时，用 **用户故事地图** 表达“用户活动 → 步骤 → 故事”，并区分最小可行范围与后续项。
   - 用 **KANO** 判断基本型、期望型、兴奋型或无差异型；若没有调研数据，明确这是暂定分类。
   - 需要比较多个候选项时，用 **RICE**：`(Reach × Impact × Confidence) / Effort`。定义每个指标的口径、时间窗和评分依据；信息不足时不要伪造分数，可使用区间或仅作定性排序。
   - 需要快速排期时，用重要/紧急四象限，并说明“重要”和“紧急”的判断依据。
4. 优先寻找低成本验证：访谈、行为数据、原型测试、灰度实验、人工运营或技术预研。每项验证写清假设、方法、成功阈值、样本/周期（若已知）和下一步决策。

## 默认交付格式

除非用户指定格式，使用以下结构并保持简洁：

1. **结论**：建议“立即做 / 先做验证 / 延后 / 不建议做”，加一句核心理由与信心等级。
2. **当前系统事实**（仅实查模式）：检查范围/角色、现有流程与关键约束、与需求相关的可复用能力或冲突点；使用脱敏描述。
3. **需求定义**：问题、目标用户、场景、拟议方案、成功指标；标注缺失信息。
4. **四维评估**：业务、用户、价值、可行性中的支持证据、风险和关键假设。
5. **方法产出**：只呈现真正用到的 5W2H、5 Whys、故事地图、KANO、RICE 或四象限结果。
6. **关键问题与反例**：包括伪需求风险、重复建设、替代方案、负面体验、指标冲突、合规与维护负担。
7. **建议路径**：最小可行方案、验证计划、验收/停止条件、优先级建议与需要业务方确认的问题。

## 判断标准

- “必要”至少应说明：不做造成的损失、现有替代方案为何不足、目标用户是否足够重要或规模足够。
- “可行”不等于“技术上能实现”；还要考虑资源、时机、组织协作、合规和可持续运营。
- 对高成本、不可逆、涉及敏感数据或合规的项目，提高证据要求，先做风险评审或小范围验证。
- 不把“领导提出”“竞品已有”“用户单次强烈要求”直接视为需求成立的证据。
- 结论与推荐的投入力度要匹配证据强度：证据弱时优先建议验证而不是直接大规模开发。
